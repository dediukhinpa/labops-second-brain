# Runtime dependencies for the second_brain MCP stack.
# Versions reflect what the upstream services are tested against (Jan 2026).

fastmcp>=2.0,<3.0
asyncpg>=0.30,<0.31
uvicorn[standard]>=0.30,<0.32
httpx>=0.27,<0.29
pyyaml>=6.0,<7.0
pydantic>=2.6,<3.0
pgvector>=0.3,<0.4
fastembed>=0.4,<1.0
numpy>=1.26,<3.0

# Test dependencies (install with pip install -r requirements.txt -r requirements-dev.txt
# or use the [dev] extra in pyproject.toml).
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<2.0
